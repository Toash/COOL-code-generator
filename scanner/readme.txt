scanner 

Reads in the program as a string, and produces a list of tokens.

test.py runs all of the tests in ./tests against the reference compiler.
test.sh does a diff against the reference compiler for the first argument.